<header>
  <div class="navbar navbar-inverse navbar-fixed-top">

    <div class="navbar-inner">
      <div class="container">
         <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
        <a href="/"><img src="/assets/logo.png" alt="Logo" class="logo pull-left"/></a>
        <a class="brand" href="/">Oats</a>
        <div class="nav-collapse collapse">
          <ul class="nav">
            <li <% if request.fullpath == '/about' %>
                 class='active' 
                 <% end %>
            >
              <a href="/about">About</a>
            </li>
            <li <% if request.fullpath == '/help' %>
                 class='active' 
                 <% end %>
            >
              <a href="/help#getting_started">Help</a>
            </li>            
            <li <% if request.fullpath == '/testimony' %>
                 class='active' 
                 <% end %>
            >
              <a href="/testimony">Testimonies</a>
            </li>
            <li <% if request.fullpath == '/contact' %>
                 class='active' 
                 <% end %>
            >
              <a href="/contact">Contact Us</a>
            </li>
            <li <% if request.fullpath == '/download' %>
                 class='active' 
                 <% end %>
            >
              <a href="/download">Download</a>
            </li>
          </ul>
          <% if user_signed_in? %>
            <ul class="nav" style="float: right; display: inline-block;">
              <li class="">
                Welcome,<a href= <%= user_path(current_user) %> style="display:inline-block"><%= current_user.name %></a>
              </li>
              <li class="">
                <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</header>